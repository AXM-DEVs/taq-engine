[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "taq-engine"
version = "1.0.1"
description = "Tactical Analysis & Qualification Engine"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
include = ["taq*"]
